# Docker Setup for Taiyo Website

## Steps:
- [x] 1. Update Dockerfile with PHP extensions, copy all code, permissions.
- [x] 2. Update docker-compose.yml with full mounts, env vars, depends_on.
- [ ] 3. docker compose up -d --build
- [ ] 4. Verify site at http://localhost:8080 and DB connection.

Progress: Files updated ✅. Run docker compose up -d --build to test.

